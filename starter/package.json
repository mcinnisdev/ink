{
  "name": "ink-starter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Ink — Markdown-native website starter built on Eleventy",
  "scripts": {
    "dev": "npx @11ty/eleventy --serve --watch",
    "build": "npx @11ty/eleventy",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
