<section class="cta-strip">
  <div class="container text-center">
    <h2 class="cta-strip__title">{{ cta_title or "Ready to get started?" }}</h2>
    <p class="cta-strip__text">{{ cta_text or "Get in touch today to learn how we can help." }}</p>
    <a href="{{ cta_url or '/contact/' }}" class="btn btn--primary">{{ cta_label or "Contact Us" }}</a>
  </div>
</section>
