{
  "name": "ink-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Official marketing and documentation site for Ink — built with Ink itself.",
  "scripts": {
    "dev": "npx @11ty/eleventy --serve --watch",
    "build": "npx @11ty/eleventy",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/eleventy-plugin-syntaxhighlight": "^5.0.2"
  }
}
