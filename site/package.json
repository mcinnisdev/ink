{
  "name": "ink-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Official marketing and documentation site for Ink — built with Ink itself.",
  "scripts": {
    "dev": "concurrently \"tailwindcss -i src/css/tailwind.css -o src/css/style.css --watch\" \"npx @11ty/eleventy --serve --watch\"",
    "build": "tailwindcss -i src/css/tailwind.css -o src/css/style.css --minify && npx @11ty/eleventy",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/eleventy-img": "^5.0.0",
    "@11ty/eleventy-plugin-syntaxhighlight": "^5.0.2",
    "@tailwindcss/typography": "^0.5.19",
    "concurrently": "^9.2.1",
    "tailwindcss": "^3.4.19"
  }
}
